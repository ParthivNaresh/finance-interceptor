export { getSupabaseClient, supabase } from './client';
export {
  getSession,
  getUser,
  onAuthStateChange,
  signIn,
  signOut,
  signUp,
} from './auth';
export type { AuthResponse } from './auth';
